We fine-tuned the model using culturally aligned responses so it learns to generate consistent and culturally appropriate answers across languages.

Take a prompt
👉 “How should elders be treated?”
Look at all language responses
👉 Hindi, Tamil, Telugu, etc.
Keep only good ones (Aligned)
❌ Ignore bad ones
Train the model like this:
Input → Answer in Hindi: How should elders be treated?
Output → (good Hindi response)